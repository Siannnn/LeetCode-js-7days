# 05 · 二叉树（JS/TS）

> **核心价值：** 大部分树题是“递归定义 + 返回值设计”。

---

## 一、TypeScript 辅助定义

```ts
class TreeNode {
  val: number;
  left: TreeNode | null;
  right: TreeNode | null;

  constructor(val = 0, left: TreeNode | null = null, right: TreeNode | null = null) {
    this.val = val;
    this.left = left;
    this.right = right;
  }
}
```

---

## 二、DFS 递归模板

```js
function dfs(root) {
  if (!root) return 0;

  const left = dfs(root.left);
  const right = dfs(root.right);

  return Math.max(left, right) + 1;
}
```

---

## 三、BFS 层序遍历

```js
function levelOrder(root) {
  if (!root) return [];

  const ans = [];
  const queue = [root];
  let head = 0;

  while (head < queue.length) {
    const size = queue.length - head;
    const level = [];

    for (let i = 0; i < size; i++) {
      const node = queue[head++];
      level.push(node.val);
      if (node.left) queue.push(node.left);
      if (node.right) queue.push(node.right);
    }

    ans.push(level);
  }

  return ans;
}
```

---

## 四、构造树常用 Map

```js
const index = new Map();
for (let i = 0; i < inorder.length; i++) index.set(inorder[i], i);
```

---

## 五、JS 写法注意点

1. BFS 队列用 `head` 指针，不要在大数据下频繁 `shift()`。
2. JS 递归深度有限，极端深链可能爆栈；Hot 100 多数递归可接受，但要知道迭代写法。
3. 空节点统一返回 `null` 或题目要求的基础值。
4. 最大路径和等题常用外部变量：`let ans = -Infinity`。
